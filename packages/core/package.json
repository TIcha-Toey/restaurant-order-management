{
  "name": "@restaurant/core",
  "version": "0.1.0",
  "description": "Business logic สำหรับระบบจัดการออเดอร์ร้านอาหาร",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@restaurant/types": "file:../types"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^3.2.1",
    "fast-check": "^4.1.1"
  }
}
